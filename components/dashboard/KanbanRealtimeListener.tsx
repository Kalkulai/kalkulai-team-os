// Renamed to DashboardRefreshController (covers Kanban broadcast + visibility-wake + polling).
// Kept as re-export so existing imports keep working — migrate when convenient.
export { KanbanRealtimeListener, DashboardRefreshController } from './DashboardRefreshController';
